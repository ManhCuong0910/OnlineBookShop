const path = {
  home: "/",
  login: "/login",
  register: "/register",
  contactPage: "/contact",
  bookPage: "/book",
  admin: "/admin",
  notPermitted: "/notPermitted",
} as const;

export default path;
